# Copy to .env — restart Vite / API after changes.

# Frontend
VITE_SITE_ORIGIN=https://pi360.net
VITE_INSTITUTE_ID=mietjammu
VITE_API_BASE=/api

# Backend (Node.js + MongoDB)
MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster.mongodb.net/initiative_tracker
JWT_SECRET="auth@pi360.net#54321!!"
JWT_ISSUER=https://pi360.net
JWT_AUDIENCE=Pi360-User
PORT=3001

# If the built app is served from a subpath (e.g. /tracker/), set:
# VITE_BASE_PATH=/tracker
